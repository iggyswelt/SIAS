# Pythia — Heartbeat
Interval: DISABLED (On-Demand)

## Aufruf-Trigger:
1. Metamaus fordert einen Audit für einen erledigten Task an.
2. Ein Agent (z.B. Hermes) liefert visuelle Daten, die validiert werden müssen.
3. System-Check: Prüfung der agent_knowledge Einträge auf Plausibilität.
