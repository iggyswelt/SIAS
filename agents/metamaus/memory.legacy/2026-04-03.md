# 2026-04-03 — Operation UNCOVER Nacht-Session

## Night Session Zusammenfassung (02.04. 22:00 MESZ – 03.04. 03:06 MESZ)

### Iggy's Direktiven:
- **MARATHON, nicht Sprint** — sequenziell arbeiten, max 1 Apollon-Task gleichzeitig
- **Athene verifiziert JEDEN Fix** bevor nächster Task startet
- **NIEMALS PROD Deploy ohne Iggys explizites "deploy freigegeben"**
- **Kein Config-Schreiben ohne Schema-Check** (contextWindow, maxTokens, compaction werden in 3.13 NICHT erkannt → Gateway-Crash!)
- **Rheingold-Tab PROD = Referenz** — DEV darf nie überschreiben

### Fixes abgeschlossen:
1. **Zerberus Cron Fix** — agentId von metamaus → zerberus geändert
2. **FIX 3: Landing Stats live** — hardcoded 59MB → 134MB live aus PostgreSQL
3. **FIX 4: Logs alle 9 Agents** — alle vorhanden, keine fehlenden
4. **Rheingold-Tab DEV** — war unterschiedlich zu PROD → PROD Version kopiert
5. **BOOTSTRAP.md** — PROD DEPLOY REGEL hinzugefügt
6. **SOUL.md** — SIAS Feedback Loop eingetragen
7. **Bug #1: Token-Anzeige** — hardcoded 1205332 → total_in + total_out (Zeile 1553 in DEV app.py)
8. **Context Config** — agents.defaults.contextTokens = 1M, contextPruning = cache-ttl
9. **Memory Config** — memory.backend = builtin (bereits gesetzt)
10. **Apollon Config** — think=off, reasoning=off (schneller)
11. **Agents Tab Fix** — valid_agents von 4 auf 9 erweitert

### Apollon-Problem:
- **Subagents hängen regelmäßig** — timeout nach 60-90 Sek ohne Output
- Ursache: Modell-Antwort-Timeout / Loop, nicht think/reasoning
- Workaround: Direkte Bash-Skripte via /tmp/ statt Apollon-Spawn
- Script-Files verwendet: /tmp/fix1_config.py, /tmp/fix_context.py, /tmp/bug2_check.sh, /tmp/dev_restart_test.sh, /tmp/dev_start.sh

### Bug-Status:
| # | Bug | Status |
|---|---|---|
| 1 | Token-Anzeige hardcoded | ✅ GEFIXT (live total_in + total_out) |
| 2 | Top-3 Agents Token | ⏳ DB-Check done: token_usage_history hat agent_name-Daten |
| 3 | Top-Tools | ❌ nicht angegangen |
| 4 | Demo-Kalender Filterung | ❌ |
| 5 | Taskboard Kategorien | ❌ |
| 6 | YouTube veraltet | ❌ |
| 7 | News Filterung | ❌ |
| 8 | Agent Report leer | ❌ |
| 9 | Agent Model in Card | ❌ |

### Bug #2 Root Cause:
- Endpoint `/api/stats/agents` queryt `minimax_usage` nach `model` (keine Agent-Namen)
- **Lösung:** Query auf `token_usage_history` mit `agent_name` umstellen
- Empfohlene Query: `SELECT agent_name, SUM(tokens_in + tokens_out) as total FROM token_usage_history WHERE created_at >= NOW() - INTERVAL '24 hours' GROUP BY agent_name ORDER BY total DESC LIMIT 5`

### MiniMax-Cluster-Probleme (tagsüber 02.04.):
- 2064 Error (Server under high load) trat mehrmals auf (09:03–11:01 UTC), dann stabilisiert
- Externes Problem — kein lokaler Fix möglich

### Gateway-Crash durch Config:
- `contextWindow`, `maxTokens`, `compaction` als Root-Level oder Agent-Level Keys werden in OpenClaw 2026.3.13 NICHT unterstützt
- Gateway startet NICHT mit diesen invalid Keys → Config sofort validieren nach Änderungen!
- Korrekte Keys: `agents.defaults.contextTokens` (nicht `contextWindow`), `agents.defaults.contextPruning`
- Validierung: `openclaw config validate` (muss 0 Issues zeigen)

### DEV Dashboard:
- Läuft auf Port 5001, PID dynamisch
- Restart: `pkill -f 5001` dann `cd /opt/dashboard-dev && python3 app.py --port 5001 &`
- `/api/stats/openclaw` Endpoint: Zeile ~1495-1615, liefert gateway stats + token_usage
- `/api/stats/agents` Endpoint: Zeile ~1616-1689, queryt minimax_usage → muss zu token_usage_history
# 2026-04-03 — Operation UNCOVER Nacht-Session

## Night Session Zusammenfassung (02.04. 22:00 MESZ – 03.04. 03:06 MESZ)

### Iggy's Direktiven:
- **MARATHON, nicht Sprint** — sequenziell arbeiten, max 1 Apollon-Task gleichzeitig
- **Athene verifiziert JEDEN Fix** bevor nächster Task startet
- **NIEMALS PROD Deploy ohne Iggys explizites "deploy freigegeben"**
- **Kein Config-Schreiben ohne Schema-Check** (contextWindow, maxTokens, compaction werden in 3.13 NICHT erkannt → Gateway-Crash!)
- **Rheingold-Tab PROD = Referenz** — DEV darf nie überschreiben

### Fixes abgeschlossen:
1. **Zerberus Cron Fix** — agentId von metamaus → zerberus geändert
2. **FIX 3: Landing Stats live** — hardcoded 59MB → 134MB live aus PostgreSQL
3. **FIX 4: Logs alle 9 Agents** — alle vorhanden, keine fehlenden
4. **Rheingold-Tab DEV** — war unterschiedlich zu PROD → PROD Version kopiert
5. **BOOTSTRAP.md** — PROD DEPLOY REGEL hinzugefügt
6. **SOUL.md** — SIAS Feedback Loop eingetragen
7. **Bug #1: Token-Anzeige** — hardcoded 1205332 → total_in + total_out (Zeile 1553 in DEV app.py)
8. **Context Config** — agents.defaults.contextTokens = 1M, contextPruning = cache-ttl
9. **Memory Config** — memory.backend = builtin (bereits gesetzt)
10. **Apollon Config** — think=off, reasoning=off (schneller)
11. **Agents Tab Fix** — valid_agents von 4 auf 9 erweitert

### Apollon-Problem:
- **Subagents hängen regelmäßig** — timeout nach 60-90 Sek ohne Output
- Ursache: Modell-Antwort-Timeout / Loop, nicht think/reasoning
- Workaround: Direkte Bash-Skripte via /tmp/ statt Apollon-Spawn
- Script-Files verwendet: /tmp/fix1_config.py, /tmp/fix_context.py, /tmp/bug2_check.sh, /tmp/dev_restart_test.sh, /tmp/dev_start.sh

### Bug-Status:
| # | Bug | Status |
|---|---|---|
| 1 | Token-Anzeige hardcoded | ✅ GEFIXT (live total_in + total_out) |
| 2 | Top-3 Agents Token | ⏳ DB-Check done: token_usage_history hat agent_name-Daten |
| 3 | Top-Tools | ❌ nicht angegangen |
| 4 | Demo-Kalender Filterung | ❌ |
| 5 | Taskboard Kategorien | ❌ |
| 6 | YouTube veraltet | ❌ |
| 7 | News Filterung | ❌ |
| 8 | Agent Report leer | ❌ |
| 9 | Agent Model in Card | ❌ |

### Bug #2 Root Cause:
- Endpoint `/api/stats/agents` queryt `minimax_usage` nach `model` (keine Agent-Namen)
- **Lösung:** Query auf `token_usage_history` mit `agent_name` umstellen
- Empfohlene Query: `SELECT agent_name, SUM(tokens_in + tokens_out) as total FROM token_usage_history WHERE created_at >= NOW() - INTERVAL '24 hours' GROUP BY agent_name ORDER BY total DESC LIMIT 5`

### MiniMax-Cluster-Probleme (tagsüber 02.04.):
- 2064 Error (Server under high load) trat mehrmals auf (09:03–11:01 UTC), dann stabilisiert
- Externes Problem — kein lokaler Fix möglich

### Gateway-Crash durch Config:
- `contextWindow`, `maxTokens`, `compaction` als Root-Level oder Agent-Level Keys werden in OpenClaw 2026.3.13 NICHT unterstützt
- Gateway startet NICHT mit diesen invalid Keys → Config sofort validieren nach Änderungen!
- Korrekte Keys: `agents.defaults.contextTokens` (nicht `contextWindow`), `agents.defaults.contextPruning`
- Validierung: `openclaw config validate` (muss 0 Issues zeigen)

### DEV Dashboard:
- Läuft auf Port 5001, PID dynamisch
- Restart: `pkill -f 5001` dann `cd /opt/dashboard-dev && python3 app.py --port 5001 &`
- `/api/stats/openclaw` Endpoint: Zeile ~1495-1615, liefert gateway stats + token_usage
- `/api/stats/agents` Endpoint: Zeile ~1616-1689, queryt minimax_usage → muss zu token_usage_history

---

## Abend-Session (03.04. ab ~17:00 MESZ)

### Dashboard systemd Migration ✅
- **dashboard-prod.service** (Port 5000) & **dashboard-dev.service** (Port 5001) erstellt & enabled
- Legacy-Prozess (host='127.0.0.1') blockierte Port 5000 → gekillt
- Auto-restart & Reboot-Persistenz aktiviert
- **Zerberus #7 Verifikation:** Ports 5000/5001 binden strikt an `127.0.0.1` ✅

### KRITISCHE SICHERHEITSREGEL (in MEMORY.md):
**NIEMALS host='127.0.0.1' für Dashboards!** Immer `127.0.0.1` binding verifizieren.

### IPFS 404 Investigation ✅
- Apollon scannte `/opt/dashboard-dev/app.py`, `index.html`, static JS
- **KEIN** interner IPFS/Kubo-Code gefunden
- 404 stammt von extern: **IPFS Companion Browser Extension** interceptet localhost-Requests
- **Lösung:** IPFS-Stub in DEV eingebaut (Zeile 3301 in app.py):
  ```python
  @app.route('/api/v0/swarm/peers', methods=['GET','POST'])
  def swarm_peers_stub():
      return {"peers": []}, 200
  ```
- PROD unberührt!

### Zerberus Security-Patches (Task #6) ✅
- 6 Pakete identifiziert (Kernel 6.17.0-20, libarchive, libc-dev)
- **⏳ Wartend auf Iggys Install-Freigabe**

### Zerberus Night Report (19:10 MESZ) ✅
- Gateway DEV/PROD: 200, PostgreSQL: accepting
- Disk: 168GB frei (25%), RAM openclaw: 9.4%
- Keine Fehler/Warnungen

### Task Queue Status:
| ID | Agent | Task | Status |
|----|-------|------|--------|
| 7 | zerberus | 127.0.0.1 Check | ✅ done |
| 8 | apollon | Tailscale DEV Access | ✅ done (Analyse) |
| 9 | apollon | Bug #8 Analyse | ✅ done |
| 10 | apollon | Bug #8 Fix | ✅ done |
| 11 | zerberus | Cronos Connectivity | ✅ done |
| 12 | athene | Trading Research | ✅ done |

### DEV RESET ✅
- Checkpoint `pre_dev_reset` erstellt (75M)
- DEV Ordner bereinigt, PROD→DEV saubere Kopie (1,8 GB)
- DEV konfiguriert auf Port 5001, `127.0.0.1`
- Beide Dashboards: HTTP 200 ✅

### Bug #8 (Agent Report leer) — ANALYSE ✅
- Endpoint `/api/agents/<agent>/report` (Zeile 1872 in DEV app.py) hatte nur `elif` für 4 Agenten
- **5 fehlende Agenten:** metamaus, apollon, rheingold, pythia, hermes → leerer String

### Bug #8 Fix ✅ (Option B: Generischer Fallback)
- `else`-Block nach `hestia`-Zweig eingefügt
- Holt letzte 5 Tasks aus `agent_tasks` + letzte 5 Logs aus `agent_logs`
- Baut HTML mit Emoji-Status (✅/⏳/📋 / 🔴/🟡/📝)
- **Alle 9 Agents liefern jetzt Report!**
- Backup: `app.py.bak` vorhanden
- Hinweis: Athene hat pre-existing Bug (`created_at` in `athene_trades` existiert nicht) — nicht durch Fix verursacht

### Tailscale DEV Access — Analyse (Task #8) ✅
| Komponente | Ergebnis |
|---|---|
| Tailscale IP Server | `100.68.216.95` |
| nginx | ❌ Nicht installiert |
| DEV Dashboard | ✅ 127.0.0.1:5001 |
| UFW Port 5001 | 🔓 ALLOW von 192.168.23.0/24 |

- **Problem:** Service nur localhost, Firewall offen aber nicht erreichbar von LAN
- **3 Optionen vorgestellt:**
  - **A:** nginx Reverse Proxy (empfohlen für LAN+Tailscale)
  - **B:** SSH-Tunnel (`ssh -L 5001:localhost:5001 iggy@metamaus`)
  - **C:** App an LAN-IP binden (192.168.23.170:5001) — einfachste LAN-Option
- **⏳ Wartend auf Iggys Entscheidung**

### Cronos (192.168.23.80) — Check ✅
- Cronos: **ONLINE** (0% packet loss, RTT ~0.1ms)
- Uptime: ~1h 13min (seit 18:15)
- Freqtrade: ❌ Weder systemd-Service noch Docker-Container gefunden
- HTTP 8080: ❌ Nicht erreichbar ⚠️ **Iggy muss prüfen ob Freqtrade installiert**

### Athene Arbitrage Research (Task #12) ✅
- Gespeichert in `agent_knowledge` (category: trading_research)
- 3 Strategien: Triangular (❌), Cross-Exchange (⚠️), Funding Rate (✅), Empfehlung: Stat-arb (Cointegration-Pairs)
- Kein Live-Trading, keine API-Keys

### Athene QC Reports:
- 17:30 → GRÜN (CPU 53,9% | RAM 40,4%)
- 18:00 → GRÜN (CPU 61% | RAM 40,6%)
- 18:30 → GRÜN (CPU 52,5% | RAM 40,1%)
- 19:00 → GRÜN (CPU 33,5% | RAM 41,4%)
- 19:30 → GRÜN (CPU 65,6% | RAM 42,3%)
- 20:00 → GRÜN (CPU 30,3% | RAM 44,6%)

### Heartbeats: 17:48, 18:19, 18:50, 19:21, 20:23 → alle HEARTBEAT_OK

### Aktiver Workflow:
1. ⏳ Iggy Tailscale-Option wählen (A/B/C)
2. ⏳ Iggy Security-Patches freigeben (apt upgrade)
3. ⏳ Iggy Freqtrade/ Cronos Status prüfen

---

## 20:30 Update

### Agent Model in Cards ✅ (Bug #9)
- Models aus `openclaw.json` in `/api/agents` Response eingebaut
- Jeder Agent hat jetzt `"model": "..."` im JSON
- DEV 200 ✅ | PROD 200 ✅ | Binding 127.0.0.1 ✅

### ⚠️ DB-Problem entdeckt:
- `athena` (DB) vs `athene` (Config) → Name-Mismatch → Model zeigt "unknown"
- Muss in DB korrigiert werden: `UPDATE agent_tasks SET agent='athene' WHERE agent='athena'`
- Ggf. auch `agent_logs`, `agent_knowledge` prüfen

### QC Cycle 20:30 ✅
- 6/6 Tabs: 200 OK | CPU 30,5% | RAM 45,7%
- DB accepting | INSERT OK → **GRÜN**

### Pending-Iggy-Entscheidungen:
1. Tailscale-Option wählen (A: nginx / B: SSH-Tunnel / C: LAN-IP)
2. Security-Patches freigeben (apt upgrade)
3. Cronos Freqtrade Status prüfen
4. `athena → athene` DB-Fix freigeben

---

## 20:30–21:06 Update — Tasks 13–15, Freqtrade Cronos

### Task #13: DB `athena → athene` Fix ✅
- Agent: apollon | Priority 9
- **agent_logs:** 1 Row korrigiert (athena → athene)
- **agent_tasks:** 0 Rows (bereits korrekt)
- **agent_knowledge:** keine Agent-Spalte, nicht betroffen
- **token_usage_history:** 0 Rows athena, nicht betroffen
- ✅ `athena` kommt in keiner Tabelle mehr vor
- ⚠️ **Nebenbefund:** `zerberus` vs `Zerberus` (Groß/Klein-Schreibung inkonsistent in agent_logs)

### Task #14: Cronos Freqtrade Status ✅ (mit Problem)
- Agent: athene | Priority 8
- **SSH-Approval-Schleife:** Athene hing 6x fest, jedes neuer SSH-Befehl brauchte neues /approve
- **Direkter SSH-Check (approved 5cd9ebbb) — Ergebnisse:**
  - Freqtrade: ✅ **LÄUFT** PID 2249, seit 18:50
  - Pfad: `/home/iggy/freqtrade/.venv/bin/freqtrade`
  - Config: `config.bot_01.json` (dry_run: true ✅)
  - Version: v2025.10, Python 3.12.3, CCXT 4.5.43
  - API: Port 8080 in Config aktiviert (listen_ip_address: 127.0.0.1)
  - User Data: Symlink → `/mnt/bigdata/freqtrade/user_data`
  - RAM: ~358 MB, CPU: ~20%
- **PROBLEM:** Freqtrade hängt beim Strategie-Laden!
  - Strategie: `BB_RPB_TSL_Tranz`
  - NostalgiaForInfinity-Dateien im strategies/-Verzeichnis blockieren (fehlende Module: `skopt`, veraltete `CategoricalParameter` Imports)
  - Log-Wachstum stoppte → Prozess scannt alle .py Dateien und blockiert
  - HTTP API noch nicht erreichbar

### Task #15: Freqtrade Cleanup Cronos 🔄 (laufend)
- Queue-Eintrag direkt von Iggy via psql INSERT
- Agent: athene | Priority 9
- Task: PID 2249 killen → Defekte NFI-Strategien nach `strategies_disabled/` verschieben → Freqtrade neu starten mit `--strategy BB_RPB_TSL_Tranz` → API ping prüfen
- DB updated auf 'running'
- Dispatched via sessions_spawn

### QC 21:00 ✅
- 6/6 Tabs: 200 OK
- CPU 41,1% | RAM 51,7%
- DB: accepting ✅
- Status: GRÜN 🟢

### Offene Iggy-Entscheidungen:
1. Tailscale-Option (A: nginx / B: SSH / C: LAN-IP)
2. Security-Patches (apt upgrade)
3. `zerberus`/`Zerberus` case fix (optional)

### Rollen-Trennung ✅ (Task #16)
| Agent | Alt | Neu |
|-------|-----|-----|
| Athene | QC + Trading | **NUR Trading/Backtest/Freqtrade** |
| Hermes | Scraping | **Scraping + QC/Verify** (alle 30min) |
- Athene Heartbeat: alle 120min
- Hermes QC: curl 6 Tabs + API + DB → agent_logs

---

## 21:06–21:35 Update — Tasks Complete

### Task #15 ✅ Freqtrade Cleanup Cronos
- PID 2249 gekillt (SIGKILL)
- **26** NostalgiaForInfinity*.py + **21** weitere Problem-Dateien nach `strategies_disabled/`
- 4 Verzeichnisse (marathon/, huge/, huge_new/, huge_strategies/ — 965+ .py) disabled
- Bugfix Zeile 393: `.extend = [...]` → `.extend([...])`
- Freqtrade neu gestartet: **PID 6261**, dry_run: true
- API ping: ✅ `{"status":"pong"}`
- ⚠️ NumPy 2.0 Warning (`np.NaN` → `np.nan`) — läuft trotzdem

### Task #16 ✅ Rollen-Trennung (QC → Hermes)
- 🏛 **Athene:** NUR Trading/Backtest/Freqtrade (Heartbeat 120min, KEIN QC)
- 🦅 **Hermes:** Scraping + QC/Verify (30min, `agent='hermes'`, `level='qc'`)
- ⚠️ Athene QC 21:30 lief noch — Hermes muss noch aktiviert werden!

### Hearbeat 21:25 → HEARTBEAT_OK 🟢
### Athene QC 21:30 → GRÜN (CPU 34,2% | RAM 44,3%)
### Offene Iggy-Entscheidungen:
1. Tailscale Option (A/B/C)
2. Security-Patches (apt upgrade)
3. Hermes QC-Heartbeat aktivieren?
4. `zerberus`/`Zerberus` case fix (optional)

---

## 21:35–22:03 Update — Tasks 17–21, Skills, QC Cron

### Task #17 🔄 Hestia — Video Transcription Cronos
- Dispatched: 2 YouTube Videos via yt-dlp + Whisper auf Cronos
- Ziel: Downloads → Whisper Transcript → DB save + metamaus Kopie
- Status: **running** (seit ~21:38) — noch offen

### Task #18 ✅ Apollon — Skills Audit
- 19 Skills im Dateisystem (16 ready, 3 broken: tesseract-ocr, toggle, x-search)
- 22/66 gelistet, 6 missing (Config)
- `openclaw skills search` defekt — CLI 2026.3.13 vs Config 2026.4.2 (Version Mismatch)
- Empfehlung: clawhub, blogwatcher, github Skills sinnvoll
- Agent-Skill-Nutzung: NUR hermes + rheingold nutzten sias-research-v1 explizit

### Task #19 ✅ Apollon — Skills in BOOTSTRAP.md aller Agents (Iggy 21:45)
| Agent | Skills eingetragen |
|-------|---------------------|
| rheingold | sias-research-v1, duckduckgo-search, nano-pdf, last30days |
| hermes | sias-research-v1, multi-search-engine, firecrawl-search |
| hestia | youtube-watcher, summarize |
| athene | yahoo-finance-cli, summarize |
| orpheus | metamaus-sql-toolkit |
| zerberus | metamaus-sql-toolkit |
| apollon | metamaus-sql-toolkit, nano-pdf |

### Task #20 ✅ Apollon — Pythia Skills nachgetragen
- Pythia: tesseract-ocr, nano-pdf, summarize, duckduckgo-search

### Task #21 ✅ Orpheus — GitHub Release v2.3.0
- CHANGELOG.md v2.3.0 Eintrag erstellt ✅
- ❌ Kein Git-Repo in `/home/iggy/.openclaw/` (nicht unter Versionskontrolle)
- Keine Commit/Tag/Push möglich
- **Offen:** Iggy muss Repo initialisieren falls gewünscht

### QC Cron Migration ✅ (21:47)
- Cron Job `d9220148` (`*/30 * * * *`): agentId von `athene-qc-30min` → `hermes-qc-30min`
- `agent='hermes'`, `level='qc'`, `level_color='info'`
- Nächste Ausführung: 22:00 MESZ
- Logs zu `agent_logs`

### Hermes QC 22:00 — erste Runde
- Cron gefeuert (22:00:28), `mail_watcher.sh` lief parallel
- Rheingold Mail-Watcher läuft alle 30min

### Heartbeat 21:56 → HEARTBEAT_OK 🟢
- PROD 5000 ✅ | DEV 5001 ✅ | Running=2 (Hestia+Orpheus) | Pending=0

### Aktuelle Queue:
| ID | Agent | Task | Status |
|----|-------|------|--------|
| ~~17~~ | hestia | 📹 Videos Cronos | 🔄 running (seit 21:38) |
| ~~18~~ | apollon | 🔍 Skills Audit | ✅ done |
| ~~19~~ | apollon | Skills BOOTSTRAP | ✅ done |
| ~~20~~ | apollon | Pythia Skills | ✅ done |
| ~~21~~ | orpheus | 🚀 GitHub Release v2.3.0 | ✅ done (CHANGELOG, kein Git-Repo) |

### Offene Iggy-Entscheidungen:
1. Tailscale Option (A: nginx / B: SSH / C: LAN-IP)
2. Security-Patches (apt upgrade)
3. GitHub Repo für .openclaw initialisieren?
4. `zerberus`/`Zerberus` case fix (optional DB cleanup)
5. Freqtrade Cronos — NumPy deprecation warnings (`np.NaN` → `np.nan`) — jetzt fixen oder später?

### Freqtrade Cronos Status:
- PID 6261, dry_run: true ✅
- API: `127.0.0.1:8080`, ping: pong ✅
- `strategies_disabled/`: 965+ Dateien verschoben
- NumPy warnings (non-blocking)

---

## 22:03–22:37 Update — Smart Throttle, Tasks 17 done, 23–25 neu

### Task #17 ✅ Hestia — Video Transcription Cronos
- yt-dlp: ✅ v2026.03.19 (bereits installiert)
- Videos: 2 MP3s (9.4 MB + 7.3 MB) auf `/mnt/bigdata/videos/`
- Whisper: ✅ Model `medium`, 50 Cores, ~38 Min.
- Transkripte: ✅ In DB (`agent_knowledge`, category `youtube_transcript`)
  - `youtube_transcript_PWkQx3cqCRE` (~10k Zeichen) — Catalyst/Enigma Arbitrage Bot
  - `youtube_transcript_09NeF22yrkQ` (~10k Zeichen) — FreqTrade Multi-Bot Dashboard

### Task #22 🔄 Apollon — Smart Throttle Dispatcher
- Neue Agent-Klassen mit unterschiedlichen Limits:
  | Klasse | Agents | Max parallel | Timeout |
  |--------|--------|-------------|---------|
  | IO | hestia, orpheus | 1 | 120min |
  | Compute | athene, apollon | 1 | 15min |
  | Fast | zerberus, rheingold, hermes | 2 | 15min |
- Script: `/home/iggy/.openclaw/agents/metamaus/scripts/dispatcher.sh`
- AUTO-TIMEOUT: Alte running Tasks → pending zurück

### Neue Tasks (22:27):
| ID | Agent | Priority | Task | Status |
|----|-------|----------|------|--------|
| **23** | athene | 9 | 💹 Arbitrage aus Video-Transkript → `SIAS_Arbitrage_v1.py` dry_run + Backtest | 🔄 dispatched |
| 24 | apollon | 8 | 🔧 Dashboard: Pythia in Cards + Model anzeigen + Gateway Token-Verbrauch | ⏳ pending |
| 25 | apollon | 7 | 🐛 DB Fix: athene_trades created_at Bug | ⏳ pending |

### Throttle State:
- Compute: 2 (athene #23 + apollon #22) → LIMIT 1 → apollon #24/#25 warten auf #22 completion
- **Maximal 4 parallel total** (Smart Throttle)

### Open:
1. Tailscale (A/B/C)
2. Security-Patches (apt upgrade)
3. GitHub Repo .openclaw initialisieren?
4. Freqtrade NumPy warnings
5. Hermes QC Reports (22:00, 22:30)

---

## 22:37–23:11 Update — Arbitrage, Docker Compose, API Tunnel, Throttle

### Task #17 ✅ Hestia — Videos Cronos (46 Min.)
- yt-dlp v2026.03.19 → 2 MP3s (9.4 + 7.3 MB)
- Whisper medium: ~38 Min., 50 Cores
- Transkripte in DB: ~10k Zeichen je Video

### Task #23 ✅ Athene — Arbitrage-Strategie
- `SIAS_Arbitrage_v1.py` → Pair-Correlation Mean-Reversion
- **Backtest: -89.94% (3,425 Trades)** — NICHT profitabel
- Video selbst: "proof of concept, not gonna make you money"
- Optimierungspotenzial: Hyperopt, höheres Timeframe, Spread-Threshold

### Task #26 ✅ Athene — Docker Compose Multi-Bot
- `/mnt/bigdata/freqtrade/docker-compose-multibot.yml` erstellt
- Services: bot01 (8081), bot02 (8082), traefik (80/443)
- ✅ NICHT gestartet — nur Datei

### Task #28 ✅ Athene — Freqtrade API Tunnel
- systemd `freqtrade-tunnel.service` running, enabled
- `curl localhost:8080/api/v1/ping` → pong
- nur 127.0.0.1 binding

### Smart Throttle DISPATCHER (v2.0 — 22:40)
- Per-Agent Throttle statt Klassen
- MAX 1 Task pro Agent gleichzeitig
- MAX 4 Tasks gesamt
- Timeouts: IO (180m) > Athene (60m) > Fast (15m)
- Script: `scripts/dispatcher.sh` (direkt geschrieben)

### Queue Status:
| ID | Agent | Task | Status |
|----|-------|------|--------|
| ~~23~~ | athene | Arbitrage | ✅ done |
| ~~26~~ | athene | Docker Compose | ✅ done |
| ~~28~~ | athene | API Tunnel | ✅ done |
| 24 | apollon | Dashboard 3 Fixes | 🔄 running |
| 25 | apollon | DB Fix athene_trades | 🔄 running |
| 27 | apollon | Smart Throttle verify | 🔄 running (?) |

### Heartbeat 23:00 → HEARTBEAT_OK 🟢
- Gateway ✅ | PROD ✅ | DEV ✅ | Freqtrade ✅

### Offen:
1. Tailscale (A/B/C) — Iggy entscheidet
2. Security-Patches — apt upgrade
3. GitHub Repo .openclaw initialisieren?
4. Hermes QC 22:30 Report?
5. Arbitrage: Hyperopt auf 1h/4h Timeframe?
